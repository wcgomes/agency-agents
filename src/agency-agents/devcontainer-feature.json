{
  "id": "agency-agents",
  "version": "0.1.0",
  "name": "Agency Agents",
  "description": "Downloads agency-agents from msitarzewski repos and runs convert/install with auto-detection by default.",
  "documentationURL": "https://github.com/msitarzewski/agency-agents",
  "options": {
    "tool": {
      "type": "string",
      "default": "auto",
      "description": "Tool name passed to ./scripts/install.sh --tool <tool>. Use 'auto' for --parallel auto-detection."
    }
  },
  "installsAfter": [
    "ghcr.io/devcontainers/features/common-utils"
  ]
}
