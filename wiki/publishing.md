# Publishing

Features published to GHCR. Release workflow runs on tag push.

Tag format: `feature_<name>_<version>`
- Example: `feature_opencode_0.2.0`

Package format: `ghcr.io/devcontainer-features/<name>:<version>`